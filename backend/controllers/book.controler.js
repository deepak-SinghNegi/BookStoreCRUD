import {Book} from '..\models\book.model.js'



const handleBookStoreController = (req , res) =>{
    try {
        const body = req.body;
    } catch (error) {
        
    }
}
export default handleBookStoreController;